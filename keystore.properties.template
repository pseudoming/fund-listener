# Release signing keystore 配置。复制此模板为 keystore.properties 填写真实路径。
# 生成 keystore 命令:
#   keytool -genkey -v -keystore fund-listener.jks -keyalg RSA -keysize 2048 -validity 10000 -alias fundlistener
#
# storeFile=../fund-listener.jks
# storePassword=your_store_password
# keyAlias=fundlistener
# keyPassword=your_key_password
