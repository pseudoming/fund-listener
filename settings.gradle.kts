rootProject.name = "fund-listener"

include(":shared")
include(":jvm-app")

// Android module — requires Android SDK.
// Uncomment and run './gradlew :android-app:assembleDebug' after installing Android SDK.
// include(":android-app")
